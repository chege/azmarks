[tool.poetry]
name = "azure-bookmarks-tool"
version = "0.1.0"
description = ""
authors = ["chege"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
azure-identity = "^1.19.0"
azure-mgmt-resource = "^23.1.1"
click = "^8.1.7"
pyyaml = "^6.0.2"

[tool.poetry.group.dev.dependencies]
black = "^24.10.0"
isort = "^5.13.2"
pytest = "^8.3.3"

[tool.poetry.scripts]
azure-bookmarks-tool = "azure_bookmarks_tool.__main__:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
